# PROJECT_ANALYSIS

## 1) 프로젝트 한 줄 요약
- **코드 기반 확인**: Raspberry Pi에서 YOLO(Hailo 가속) + MediaPipe Pose를 병렬 처리하고, 결과를 STM32/웹으로 전달해 이상행동에 대응하는 실시간 비전 로봇 파이프라인입니다.

## 2) 프로젝트 목적
- **코드 기반 확인**: 카메라 프레임에서 사람/행동을 탐지·추적하고(`hailo_worker`), 자세 기반 규칙 검증(`mediapipe_worker` + `rapi_logics`)을 거쳐, 하드웨어 제어 신호 및 웹 상태 전송을 수행합니다.
- **코드 기반 확인**: 단순 객체 탐지 결과를 바로 사용하는 것이 아니라, 프레임 누적 카운트와 포즈 규칙을 결합해 경보/확정 단계를 나누어 오탐을 줄이려는 구조입니다.
- **추정**: 공공장소 순찰/안전 관제 시나리오에서 “탐지 후 즉시 대응” 자동화를 목표로 설계된 프로젝트로 보입니다.

## 3) 전체 시스템 구조
- **코드 기반 확인**: `main.py`가 중앙 오케스트레이터이며, 큐 기반으로 3개 워커(Hailo, MediaPipe, Hardware)를 연결합니다.
  - Main → Hailo: 원본 프레임 전달
  - Hailo → Main: 탐지/트래킹 결과 전달
  - Hailo → MediaPipe: 사람 크롭 전달
  - MediaPipe → Main: 랜드마크 전달
  - Main → Hardware: STM32 제어 명령 + 웹 전송 데이터 전달
- **코드 기반 확인**: 멀티프로세싱/멀티스레딩 전환이 설정값(`USE_MULTIPROCESSING`)으로 가능하며 기본은 멀티프로세싱입니다.
- **추정**: 실제 물리 로봇에서는 STM32가 주행/경보를 담당하고 Raspberry Pi는 비전 인지와 상위 의사결정을 담당하는 2계층 제어 구조입니다.

## 4) 주요 폴더/파일 설명
- `src/main.py`
  - **코드 기반 확인**: 실행 엔트리포인트. 워커 생성, 카메라 루프, 상태 집계, 행동 판별, 큐 라우팅, 화면 렌더링 담당.
- `src/config.py`
  - **코드 기반 확인**: 모델 경로, 클래스 정의, 임계값, 웹 전송 URL, 실행 모드 설정.
- `src/workers/hailo_worker.py`
  - **코드 기반 확인**: Hailo 추론 + ByteTrack 추적 + person crop 생성.
- `src/workers/mediapipe_worker.py`
  - **코드 기반 확인**: crop 단위 Pose 추론 후 랜드마크 반환.
- `src/workers/hardware_worker.py`
  - **코드 기반 확인**: GPIO bit 신호 출력(STM32 전달), 웹 상태/영상 POST, 내부 스레드 분리(servo/web).
- `utils/utils.py`
  - **코드 기반 확인**: 레터박스 리사이즈, 랜드마크 오버레이, 트랙 히스토리 기반 크롭/프레임 누적 관리.
- `utils/rapi_logics.py`
  - **코드 기반 확인**: attack/smoke/falldown 규칙 기반 판별 로직.
- `sever/templates/index.html`
  - **코드 기반 확인**: 모니터링 대시보드 프론트(영상, 알람, 통계, 시스템 정보 UI).
- `stm32/vision_Ai_robot_Guide.c`
  - **추정**: STM32 펌웨어/가이드 코드로 보이며, 저장소 내 Python 시스템과 연동되는 MCU 측 구현 파일일 가능성이 큽니다.

## 5) 실행 흐름
1. **코드 기반 확인**: `main.py` 실행 시 워커 프로세스(또는 스레드)와 큐를 초기화합니다.
2. **코드 기반 확인**: 웹캠 프레임을 읽어 `frame_queue`로 전달합니다.
3. **코드 기반 확인**: `hailo_worker`가 추론/트래킹 후 결과를 `result_queue`로, 사람 crop을 `cropped_queue`로 보냅니다.
4. **코드 기반 확인**: `mediapipe_worker`가 crop에서 랜드마크를 추출해 `result_queue`로 반환합니다.
5. **코드 기반 확인**: 메인이 탐지+포즈를 합쳐 행동 상태를 판정하고 command/web 메시지를 구성합니다.
6. **코드 기반 확인**: `hardware_worker`가 STM32용 비트 신호를 주기적으로 출력하고(Alive 포함), 필요 시 웹 상태/영상을 POST 전송합니다.
7. **추정**: 최종적으로 로봇은 추적 이동/경보 장치(LED, 부저, 레이저 등) 동작을 상황 단계별로 수행합니다.

## 6) 핵심 모듈 설명
- `hailo_worker`
  - **코드 기반 확인**: HEF 로드 → Hailo VDevice 구성 → 추론 → 레터박스 역변환 → ByteTrack ID 부여.
  - **코드 기반 확인**: `get_person_crops`를 통해 track_id별 누적 프레임/미스 프레임을 관리하고 포즈용 크롭 생성.
- `mediapipe_worker`
  - **코드 기반 확인**: MediaPipe PoseLandmarker(VIDEO mode)로 랜드마크를 track_id와 함께 반환.
- `rapi_logics`
  - **코드 기반 확인**: 관절 각도/거리 기반 규칙식으로 공격/흡연/낙상 여부를 판별.
- `hardware_worker`
  - **코드 기반 확인**: 탐지 상태를 `STATE_MAP`으로 매핑하고, 박스 중심/크기 기반으로 이동 명령 계산 후 GPIO로 송신.
  - **코드 기반 확인**: 웹 명령 전송과 영상 스트림 전송 스레드를 분리하여 비동기 처리.

## 7) 사용 기술 스택
- **코드 기반 확인**: Python (멀티프로세싱/스레딩, 큐, OpenCV, requests)
- **코드 기반 확인**: Hailo SDK(`hailo_platform`), MediaPipe Tasks, Supervision(ByteTrack)
- **코드 기반 확인**: Raspberry Pi GPIO 제어(`gpiozero`)
- **코드 기반 확인**: HTML/CSS 대시보드 템플릿
- **추정**: 임베디드 측은 STM32(C/C++) 펌웨어와 GPIO 병렬 프로토콜로 연동됩니다.

## 8) 코드에서 확인되는 주요 기능
- **코드 기반 확인**: 실시간 객체 탐지 및 추적 (Hailo + ByteTrack)
- **코드 기반 확인**: track_id 기반 포즈 2차 검증(크롭 최적화)
- **코드 기반 확인**: 프레임 누적 임계값(의심/확정)에 따른 이벤트 단계화
- **코드 기반 확인**: 위치/거리 기반 로봇 이동 명령 산출
- **코드 기반 확인**: GPIO 비트 제어를 통한 STM32 명령 송신 + alive 신호
- **코드 기반 확인**: 웹 상태/영상 비동기 전송(옵션 플래그 `WEBSITE_ON`)
- **추정**: 실제 운영에서는 경보장치/모터 제어와 연계되어 물리 대응까지 완료됩니다.

## 9) 포트폴리오에 쓸 수 있는 기술 역량
- **코드 기반 확인**: 멀티프로세스 기반 실시간 비전 파이프라인 설계/구현 역량
- **코드 기반 확인**: Edge AI 추론(Hailo)과 고전 규칙(관절 기하) 결합을 통한 오탐 저감 설계 역량
- **코드 기반 확인**: 객체 추적 ID 중심 상태머신(누적 카운터/미스 카운터) 설계 역량
- **코드 기반 확인**: AI 출력값을 하드웨어 제어 신호(GPIO bitfield)로 매핑하는 시스템 통합 역량
- **코드 기반 확인**: 실시간 스트림 전송 및 모니터링 UI 연동 경험
- **추정**: 임베디드/AI/웹을 묶는 크로스도메인 협업 및 통합 디버깅 역량을 강조하기 좋습니다.

## 10) 면접 예상 질문과 답변 포인트
1. **Q. 왜 YOLO 결과만 쓰지 않고 MediaPipe를 추가했나요?**
   - **답변 포인트(코드 기반 확인)**: `is_attack/is_smoke/is_falldown` 규칙을 통해 포즈 기반 2차 검증을 수행해 오탐을 줄이기 위함.
2. **Q. 실시간성은 어떻게 확보했나요?**
   - **답변 포인트(코드 기반 확인)**: 멀티프로세싱, 짧은 큐 길이, crop 기반 포즈 추론, 비동기 워커 분리 구조 사용.
3. **Q. 흔들리는 탐지 결과를 어떻게 안정화했나요?**
   - **답변 포인트(코드 기반 확인)**: track_id별 hit/miss 카운트와 이벤트 프레임 누적 임계값으로 단계적 확정.
4. **Q. AI 결과를 실제 로봇 제어로 어떻게 연결했나요?**
   - **답변 포인트(코드 기반 확인)**: `hardware_worker`에서 상태코드/이동코드를 GPIO 비트로 매핑해 STM32로 전송.
5. **Q. 장애 상황(네트워크/프레임 누락)에 대한 대응은?**
   - **답변 포인트(코드 기반 확인)**: 큐 예외 처리, 웹 요청 timeout, 추적 객체의 단기 위치 유지(missing frame 처리).
6. **Q. 개선한다면 무엇을 보완하겠나요?**
   - **답변 포인트(추정)**: 임계값 자동 튜닝, 이벤트 로그 저장/재현, 모델 버전 관리, 서버 URL/하드웨어 설정 외부화.

## 11) 코드만으로는 확인할 수 없는 정보 목록
- **코드 기반 확인 불가**: 실제 배포 하드웨어 배선도/핀맵 상세(문서화 수준)
- **코드 기반 확인 불가**: 학습 데이터셋 구성, 라벨 품질, 데이터 수집 윤리/법적 검토
- **코드 기반 확인 불가**: 정량 성능(FPS, 지연, 정확도)의 재현 가능한 측정 환경과 실험 프로토콜
- **코드 기반 확인 불가**: STM32 펌웨어와 Python 간 실제 프로토콜 명세(비트 의미, 타이밍 요구사항)
- **코드 기반 확인 불가**: 웹 서버 백엔드 구현(현재 템플릿은 있으나 API 서버 코드 미확인)
- **코드 기반 확인 불가**: 운영 환경의 장애 대응 정책(재시작 전략, watchdog, 로깅/알림)

## 12) README 또는 포트폴리오 작성을 위해 추가로 필요한 정보
- **필수(코드 기반 확인 + 문서 보강 필요)**
  - 실행 방법(의존성 설치, 모델 파일 배치, 실행 명령)
  - 환경 요구사항(Python 버전, Hailo SDK 버전, Raspberry Pi OS)
  - `.hef`, `.task` 모델 파일 출처/버전/라이선스
  - 서버 URL 설정 방법 및 API 스펙(요청/응답 예시)
- **필수(추정)**
  - GPIO ↔ STM32 핀 매핑 표, 상태/이동 코드 테이블, 통신 타이밍 다이어그램
  - 실제 데모 시나리오(정상/오탐/다중 인물/가림 상황)
  - 성능 지표 측정 기준(해상도, 조도, 거리, 프레임률, 지연 분해)
  - 실패 사례와 개선 내역(면접/포트폴리오 신뢰도 향상)
- **권장(추정)**
  - 시스템 한계(야간, 역광, 군중 밀집)와 안전 제약 조건 명시
  - 개인정보/영상 데이터 취급 정책 및 익명화/보관 정책
